{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Option Replay",
      "type": "go",
      "request": "launch",
      "mode": "auto",
      "program": "${workspaceFolder}/cmd/option-replay",
      "args": [
        "-config",
        "${workspaceFolder}/input/strategies/custom.json"
      ],
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal"
    }
  ]
}
